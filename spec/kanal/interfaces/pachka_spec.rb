# frozen_string_literal: true

RSpec.describe Kanal::Interfaces::Pachka do
  it "has a version number" do
    expect(Kanal::Interfaces::Pachka::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
